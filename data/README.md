# Data

Source inputs for your analyses. For each dataset, document its provenance: where it came from, when it was pulled, and any transformations applied before use.

Do not commit sensitive, proprietary, or identifiable data (including any patient information) — see `.gitignore` and `AGENTS.md`.
